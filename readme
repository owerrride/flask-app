1. Install python and pip3 manager: apt-get install python3 -y, apt-get install python3-pip -y
apt install python3.11-venv
mkdir flask-app && cd flask-app
python3 -m venv myenv
source myenv/bin/activate
pip install flask
